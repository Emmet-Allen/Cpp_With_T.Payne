#include <iostream>
#include <string>
using namespace std;

int main() {
	/*1) cin would be used to store a users input into a variable, while
	getline() would be used to take in 2 arguments: user input and variable value */

	cout << ((2 + 20) - 10 * 60 / 8.2) % 2 << endl;

	return 0;
}